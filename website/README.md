# Quick SDF Paint documentation site

This directory contains the static documentation site published with GitHub Pages.

```powershell
npm install
npm run lint
npm run build
npm test
```

The production build is written to `out/` with the `/QuickSDFBlender/` base path.
